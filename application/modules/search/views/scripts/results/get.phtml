<?php

function cuttext($string, $cutoff)
{
    if (mb_strlen($string) < $cutoff) {
        return $string;
    } else {
        // look for a space
        $new_str = mb_substr($string, 0, $cutoff);
        return mb_strrchr($new_str, ' ', true) . '...';
    }
}

?>

<?php if (empty($this->data)): ?>

<div style="padding:150px;text-align:center;">нема даних :(</div>

<?php else: ?>

<div class="search-results">
<?php foreach ($this->data as $res): ?>
	<div class="search-results-item">
		<div class="title"><a href="#info-<?php echo $res->id ?>"><?php echo cuttext($res['title'], 70) ?></a></div>
		<div class="description"><?php echo cuttext($res['description'], 200) ?></div>
		<div class="url"><?php echo $res['url'] ?></div>
	</div>
<?php endforeach; ?>
</div>

<?php endif; ?>